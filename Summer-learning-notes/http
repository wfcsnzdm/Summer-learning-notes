1.媒体类型
Content-type: text/html
URI(URL,URN)
2.状态码
404
3.报文{
(a.请求报文 b.响应报文)(1.起始行
 2.首部
 3.主体)}
4.TCP/IP
HTTP:应用层
TCP:传输层
IP:网络层
网络特有的链路接口:数据链路层 
物理网络硬件:物理层
DNS(Domain Name Service)域名服务(一种机制)
5URL的三部分:方案,主机和端口(默认为80),路径
6片段
7常见的方案本语法
    下午 python的基本语法
    晚上 python分享
   http 超文本传输协议方案,除了没有用户名和密码之外,与通用的URL格式相符.如果省略端口 默认为80
   https 于http差不多只是进行了加密(使用了网景的SSL) 端口为443
   mailto 用于邮件 不指向任何可以直接访问的对象 
   ftp 文件传输协议,用来向FTP服务器端上传或者下载文件.(WEB将其作为一种访问数据的方式)
   rtsp 可以通过实时流传输协议
   rtspu 同上+用UDP来获取资源的 
   file 本地文档
   news 用来访问一个特定的文章或者新闻组 自身包含的信息不足以对资源进行定位
   telnet 
请求报文的格式
<method(方法:eg GET HEAD POST)> <request-URL 请求URL> <version 版本>
<headers>

<entity-body>
